[project]
name = "jeb-trading"
version = "0.1.0"
description = "A System One trading gym: tick data -> chart images + account state -> typed decisions on a virtual account"
requires-python = ">=3.11"
dependencies = ["pandas>=2.2", "pyarrow>=15", "numpy>=1.26", "matplotlib>=3.8", "httpx>=0.27", "pillow>=10"]

[tool.uv]
package = false
