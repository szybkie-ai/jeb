[project]
name = "jeb-doom"
version = "0.1.0"
description = "JEB plays Doom: ViZDoom engine state -> System One questions -> controls"
requires-python = ">=3.11"
dependencies = ["vizdoom>=1.2", "httpx>=0.27", "numpy>=1.26", "pillow>=10"]
